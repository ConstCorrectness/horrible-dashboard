---
title: backend.sdk — backend plugins
sidebar_position: 2
---

{/* Generated by backend/modules/symdex/gen_reference_docs.py — do not edit. Regenerate with `uv run python -m backend.modules.symdex.gen_reference_docs`. */}

# backend.sdk — backend plugins

The server-side plugin contract: routes, agent tools, `/ws` channels, lifespan hooks, connectors and `dash` facades. See [the backend plugin SDK](../architecture/backend-plugin-sdk.mdx).

Also browsable, with search, in the **Python reference** pane (`/pydoc`) under *Dashboard SDKs*.

## `backend.sdk`

### `AgentTool`

````text
AgentTool
````

````text
A server-side agent tool. Unlike browser tools, its handler runs in the
backend, so it works with no tab attached. `side_effect=True` routes it through
the agent permission gate (read-only tools pass straight through).
````

### `AgentToolHandler`

````text
AgentToolHandler
````

*Undocumented.*

### `BackendPlugin`

````text
BackendPlugin
````

````text
Base class for a backend plugin. Subclass it, set `manifest`, and implement
`setup` to register capabilities. A plugin module must expose its instance as a
module-level `PLUGIN`.
````

### `DashFacadeFactory`

````text
DashFacadeFactory
````

*Undocumented.*

### `LifecycleHook`

````text
LifecycleHook
````

*Undocumented.*

### `PluginHost`

````text
PluginHost
````

````text
The capability-registration surface handed to a plugin's `setup()`. Every
method namespaces the contribution under the plugin's id so the app can attribute
it. A plugin should call these only from `setup()`.
````

### `PluginManifest`

````text
PluginManifest
````

````text
Identity of a backend plugin.
````

### `PluginRegistry`

````text
PluginRegistry
````

````text
Everything backend plugins have contributed, aggregated across all plugins.
````

### `WsChannelHandler`

````text
WsChannelHandler
````

*Undocumented.*

### `load_plugins`

````text
load_plugins
````

````text
Discover and set up every backend plugin into `reg` (the global registry by
default). Idempotent per registry: call `reg.reset()` first to reload. Returns the
registry for convenience.
````

### `registry`

````text
registry
````

*Undocumented.*

## `backend.sdk.host`

### `BackendPlugin`

````text
class BackendPlugin
````

````text
Base class for a backend plugin. Subclass it, set `manifest`, and implement
`setup` to register capabilities. A plugin module must expose its instance as a
module-level `PLUGIN`.
````

### `PluginHost`

````text
class PluginHost
````

````text
The capability-registration surface handed to a plugin's `setup()`. Every
method namespaces the contribution under the plugin's id so the app can attribute
it. A plugin should call these only from `setup()`.
````

### `PluginHost.__init__`

````text
PluginHost.__init__(self, manifest: PluginManifest, reg: PluginRegistry | None=None) -> None
````

*Undocumented.*

### `PluginHost.add_agent`

````text
PluginHost.add_agent(self, spec: AgentSpec) -> None
````

````text
Register a specialized agent (own system prompt, tool-group scope, and
per-agent `agent.<id>.*` settings). It appears in the roster next to the
built-ins and is addressable from the chat widget and `agent.delegate`.
Built-in agent ids (main, coder, dba, researcher) cannot be overridden.
````

### `PluginHost.add_agent_tool`

````text
PluginHost.add_agent_tool(self, tool: AgentTool) -> None
````

````text
Expose a server-side agent tool. Backend plugins win name conflicts in load
order; a name clash with a core tool is the plugin author's responsibility.
````

### `PluginHost.add_connector`

````text
PluginHost.add_connector(self, connector: Connector) -> None
````

````text
Expose an external account the user can connect from the home page, and
whose credential the node holds server-side.

Name the connector's agent tools `<connector.id>.*` — the orchestrator groups
tools by their name prefix, so a mismatch silently splits the connector's
tools off from its blurb and guide.
````

### `PluginHost.add_dash_facade`

````text
PluginHost.add_dash_facade(self, name: str, factory: DashFacadeFactory) -> None
````

````text
Attach `dash.<name>` in every REPL session (factory builds the object).
````

### `PluginHost.add_dataset_source`

````text
PluginHost.add_dataset_source(self, source: Any) -> None
````

````text
Expose a place training material lives (an object satisfying
`backend.modules.datasets.sources.DatasetSource`): search, splits, peek and
locate for one source. It appears in the datasets browser alongside the Hub
and the local files, and a recipe can point at it like any other.

Built-in sources (hub, local, exports, kaggle) win id conflicts.
````

### `PluginHost.add_eval_runner`

````text
PluginHost.add_eval_runner(self, runner: Any) -> None
````

````text
Expose a way of executing an eval suite (an object satisfying
`backend.modules.evals.runners.EvalRunner`): which case types it claims and
how it runs them. Results land in the same tables as every other runner's,
so a mixed suite still adds up. Built-ins win id conflicts.
````

### `PluginHost.add_recipe_backend`

````text
PluginHost.add_recipe_backend(self, backend: Any) -> None
````

````text
Expose a training framework (an object satisfying
`backend.modules.training.backends.base.RecipeBackend`): which tasks it can
train, which knobs it takes, and the code it emits for them.

The field catalog it returns is validated against the project venv by the
same introspection every built-in backend goes through, so a knob the
installed library does not accept is dropped from the emitted code and shown
as dropped rather than emitted hopefully. Built-ins (trl, unsloth,
torchtitan, nanotron) win id conflicts.
````

### `PluginHost.add_router`

````text
PluginHost.add_router(self, router: APIRouter, *, prefix: str | None=None) -> None
````

````text
Mount a FastAPI router. Default prefix is `/api/plugins/<id>`; pass an
explicit `prefix` (joined under `/api`) to override.
````

### `PluginHost.add_search_provider`

````text
PluginHost.add_search_provider(self, provider: Any) -> None
````

````text
Expose a web-search provider (an object satisfying
`backend.modules.search.base.SearchProvider`): one `search(query, ...)` that
returns ranked `SearchResult`s. It joins the fan-out and rank fusion
alongside the built-ins, so a plugin's provider is indistinguishable from
Tavily or the node's own crawl index. Reusing a built-in's id overrides it.
````

### `PluginHost.add_training_provider`

````text
PluginHost.add_training_provider(self, provider: Any) -> None
````

````text
Expose a training environment provider (an object satisfying
`backend.modules.training.providers.base.EnvironmentProvider`): search,
resolve, fetch, and scaffold one kind of trainable environment. Built-in
providers (kaggle, huggingface, gymnasium) win id conflicts.
````

### `PluginHost.add_ws_channel`

````text
PluginHost.add_ws_channel(self, channel: str, handler: WsChannelHandler) -> None
````

````text
Handle a new channel on the shared `/ws` socket. Built-in channels
(`agent`, `collab`, …) take precedence; pick a unique channel name.
````

### `PluginHost.manifest`

````text
PluginHost.manifest(self) -> PluginManifest
````

*Undocumented.*

### `PluginHost.on_shutdown`

````text
PluginHost.on_shutdown(self, hook: LifecycleHook) -> None
````

````text
Run `hook` when the app shuts down. Sync or async.
````

### `PluginHost.on_startup`

````text
PluginHost.on_startup(self, hook: LifecycleHook) -> None
````

````text
Run `hook` when the app starts (inside the lifespan). Sync or async.
````

### `BackendPlugin.setup`

````text
BackendPlugin.setup(self, host: PluginHost) -> None
````

````text
Register this plugin's capabilities via `host`.
````

## `backend.sdk.loader`

### `load_plugins`

````text
load_plugins(*, extra_dirs: list[Path] | None=None, reg: PluginRegistry | None=None) -> PluginRegistry
````

````text
Discover and set up every backend plugin into `reg` (the global registry by
default). Idempotent per registry: call `reg.reset()` first to reload. Returns the
registry for convenience.
````

## `backend.sdk.registry`

### `PluginRegistry`

````text
class PluginRegistry
````

````text
Everything backend plugins have contributed, aggregated across all plugins.
````

### `PluginRegistry.dispatch_ws`

````text
PluginRegistry.dispatch_ws(self, conn: Any, channel: str, message: dict[str, Any]) -> bool
````

````text
Route a `/ws` frame to a plugin channel handler. Returns True if handled.
````

### `PluginRegistry.invoke_agent_tool`

````text
PluginRegistry.invoke_agent_tool(self, name: str, args: dict[str, Any]) -> Any
````

````text
Run a plugin agent tool by name, awaiting an async handler. Failures come
back as an `{'error': ...}` value so one bad tool can't break a turn.
````

### `PluginRegistry.provider_tools`

````text
PluginRegistry.provider_tools(self, *, grouped: bool | None=None) -> list[dict[str, Any]]
````

````text
Plugin agent tools as provider tool definitions (for the model).

`grouped=None` returns all (back-compat). `grouped=False` returns only
always-core tools (no `group`); `grouped=True` returns only the ones
disclosed under a group, for the progressive-disclosure pool.
````

### `PluginRegistry.reset`

````text
PluginRegistry.reset(self) -> None
````

````text
Drop all registrations (used between test loads).
````

### `PluginRegistry.run_shutdown`

````text
PluginRegistry.run_shutdown(self) -> None
````

*Undocumented.*

### `PluginRegistry.run_startup`

````text
PluginRegistry.run_startup(self) -> None
````

*Undocumented.*

## `backend.sdk.types`

### `AgentSpec`

````text
class AgentSpec
````

````text
One agent persona in the roster — a fully separate loop with its own system
prompt, tool scope, per-agent settings (`agent.<id>.*`), and conversation
history. The built-in roster (main/coder/dba/researcher) lives in
`backend.modules.agent.roster`; plugins add their own via `host.add_agent`.

`tool_groups=None` means unrestricted (every group loadable — the main
orchestrator); a list restricts the agent to those groups. `preload_groups`
are active from the first round (specialized agents skip keyword preloading,
their scope is already known).
````

### `AgentTool`

````text
class AgentTool
````

````text
A server-side agent tool. Unlike browser tools, its handler runs in the
backend, so it works with no tab attached. `side_effect=True` routes it through
the agent permission gate (read-only tools pass straight through).
````

### `Connector`

````text
class Connector
````

````text
An external account the node can connect to and hold credentials for.

`id` MUST equal the namespace of the agent tools it enables (`github` ->
`github.searchCode`), because the orchestrator derives a tool's group from its
name prefix (`_group_of`) — the `AgentTool.group` field does not name the group.
Keeping them equal is what lets one connector definition feed both the home tile
and the agent's tool-group catalog.
````

### `ConnectorAccount`

````text
class ConnectorAccount
````

````text
The connected identity, for display only — never a credential.
````

### `ConnectorScope`

````text
class ConnectorScope
````

````text
One permission the connector asks the provider for, in the user's words.
Rendered in the tile popover so "what did I grant this thing" is answerable
without reading the source.
````

### `ConnectorStatus`

````text
class ConnectorStatus
````

````text
A connector's current state. `error` is for a connection that exists but is
unusable (revoked token, unreadable credential) — distinct from `connected=False`,
which means "never connected". Collapsing the two is what makes a broken
integration look like an unconfigured one.
````

### `PluginManifest`

````text
class PluginManifest
````

````text
Identity of a backend plugin.
````

### `AgentTool.meta`

````text
AgentTool.meta(self) -> dict[str, Any]
````

````text
Permission-gate metadata (matches the shape the orchestrator expects).
````

### `AgentTool.provider_tool`

````text
AgentTool.provider_tool(self) -> dict[str, Any]
````

````text
The provider (Ollama/OpenAI) tool definition for this tool.
````

### `Connector.resolve_guide`

````text
Connector.resolve_guide(self) -> str | None
````

````text
The guide text, calling the factory if one was given.
````
