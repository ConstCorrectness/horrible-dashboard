---
title: dash — the REPL handle
sidebar_position: 1
---

{/* Generated by backend/modules/symdex/gen_reference_docs.py — do not edit. Regenerate with `uv run python -m backend.modules.symdex.gen_reference_docs`. */}

# dash — the REPL handle

`dash` is seeded into every in-app Python REPL session and scripts the running dashboard — panes, workspaces, layout, I/O, settings, git — and every module can add a `dash.<name>` facade (`dash.lens` reads llama.cpp traces as numpy). `dash.help()` prints the same list at the prompt. It is a global, not an importable module: there is no `import dash`. See [the Python SDK](../architecture/python-sdk.mdx).

Also browsable, with search, in the **Python reference** pane (`/pydoc`) under *Dashboard SDKs*.

## `dash`

### `call`

````text
call(name: str, **args: Any) -> Any
````

````text
Relay an arbitrary tool by name — covers every widget `agentTool` and
agent-exposed command, so new capabilities are reachable without an SDK
change.
````

### `context`

````text
context(instance_id: str) -> Any
````

````text
Read an active pane instance's current state/selection snapshot (instanceId from
`panes.open_list()`).
````

### `help`

````text
help() -> None
````

````text
Print the whole `dash` surface (facades, methods, and one-line docs).
````

## `dash.code`

### `find`

````text
find(query: str, limit: int=50) -> list[dict[str, Any]]
````

````text
Fuzzy symbol search across the workspace roots (exact-name / structural).
````

### `locus`

````text
locus() -> dict[str, Any]
````

````text
The current code locus (path + optional range/symbol), or `{}` if none.
````

### `reindex`

````text
reindex() -> dict[str, Any]
````

````text
Rebuild the semantic index over the workspace roots (blocks until done).
````

### `search`

````text
search(query: str, limit: int=10) -> dict[str, Any]
````

````text
Semantic search: find definitions by meaning (embeddings). Returns
`{building: True}` with no results until the index is built — run
`dash.code.reindex()` first if so.
````

### `set_locus`

````text
set_locus(path: str, line: int | None=None, symbol: str | None=None) -> dict[str, Any]
````

````text
Point the locus at `path` (optionally a 1-based `line`/`symbol`); browsers
open and scroll to it.
````

### `symbols`

````text
symbols(path: str) -> list[dict[str, Any]]
````

````text
The definitions (outline) in a file — functions, classes, methods, ….
````

## `dash.git`

### `blame`

````text
blame(path: str) -> dict[str, Any]
````

````text
Per-line authorship for a file, each line tagged with the session that wrote
its commit (if any).
````

### `commit`

````text
commit(message: str, paths: list[str] | None=None) -> dict[str, Any]
````

````text
Stage + commit, stamping the active chat session as provenance trailers.
````

### `log`

````text
log(limit: int=20) -> dict[str, Any]
````

````text
Recent commits; a `session_id` marks a commit agent-authored.
````

## `dash.io`

### `clear`

````text
clear() -> dict[str, Any]
````

````text
Empty the telemetry ring buffer.
````

### `errors`

````text
errors(limit: int=20) -> list[dict[str, Any]]
````

````text
Recent events that failed (error set, or HTTP status ≥ 400).
````

### `recent`

````text
recent(limit: int=20) -> list[dict[str, Any]]
````

````text
The most recent I/O events (newest last), each as a dict.
````

## `dash.layout`

### `arrange`

````text
arrange(style: str='grid') -> Any
````

````text
Lay every open window out: `grid`, `cascade`, `columns` or `rows`.
````

### `backdrop`

````text
backdrop(backdrop_id: str, **params: Any) -> Any
````

````text
Set the desktop backdrop. Ids are in `describe()["desktop"]["backdrops"]`.
````

### `describe`

````text
describe() -> Any
````

````text
The whole frame: center split tree (areas, tabs, regions), docks,
floating panes, fullscreen/focused area.
````

### `dock`

````text
dock(instance_id: str) -> Any
````

````text
Put a windowed pane back into the tiling frame.
````

### `fullscreen`

````text
fullscreen(instance_id: str | None=None, on: bool=True) -> Any
````

````text
Expand a pane's area to fill the frame, or restore with `on=False`.
````

### `join`

````text
join(instance_id: str, direction: str) -> Any
````

````text
Absorb the neighboring area (`left`/`right`/`up`/`down`) into this
pane's area; document tabs are adopted when both sides hold documents.
````

### `mode`

````text
mode(mode: str) -> Any
````

````text
Switch this desktop between `tiling` and `floating`.
````

### `move`

````text
move(instance_id: str, area_id: str | None=None, direction: str | None=None, edge: str | None=None) -> Any
````

````text
Move a center pane into `area_id` (from `describe()`), toward
`direction` (`left`/`right`/`up`/`down`), or onto an area's `edge`
(`left`/`right`/`above`/`below`), which splits it and lands the pane in
the new half.
````

### `open_tool`

````text
open_tool(view_id: str, dock: str | None=None) -> Any
````

````text
Open (or focus) a role:'tool' view in a dock (defaults to its own side).
````

### `resize`

````text
resize(instance_id: str, width: int | None=None, height: int | None=None) -> Any
````

````text
Resize the area holding a pane (pixels).
````

### `set_region_view`

````text
set_region_view(instance_id: str, view_id: str) -> Any
````

````text
Show a specific region view on its host pane (opens the strip).
````

### `split`

````text
split(instance_id: str, direction: str, view_id: str | None=None) -> Any
````

````text
Split the area holding a pane (`left`/`right`/`above`/`below`, or the
`vertical`/`horizontal` aliases); `view_id` to open a different view in the
new area, else the pane's own view is duplicated.
````

### `theme`

````text
theme(theme_id: str) -> Any
````

````text
Switch the app theme. Ids are in `describe()["desktop"]["themes"]`.
````

### `toggle_dock`

````text
toggle_dock(dock: str, visible: bool | None=None) -> Any
````

````text
Show/hide a dock (`left`/`right`/`bottom`); `visible` forces a state.
````

### `toggle_region`

````text
toggle_region(instance_id: str, position: str, open: bool | None=None) -> Any
````

````text
Toggle a pane's region strip (`left`/`right`/`bottom`); `open` forces a state.
````

### `window`

````text
window(instance_id: str, snap: str | None=None, rect: dict[str, float] | None=None) -> Any
````

````text
Pop a pane out into a free-floating desktop window.

`snap` places it in a screen region (`left`/`right`/`top`/`bottom`, a
corner, or `max`); `rect` gives exact pixels. Windows work on a tiling
desktop too — there they are the escape hatch for a pane that should not
participate in the tiling.
````

### `window_state`

````text
window_state(instance_id: str, state: str, snap: str | None=None, workspace_id: str | None=None) -> Any
````

````text
`minimize`, `maximize`, `restore`, `snap` or `move_to_desktop`.

A minimized window keeps running — it is hidden, not closed.
````

## `dash.lens`

### `attention`

````text
attention(trace_id: str, layer: int, head: int=0, pass_index: int=0) -> Any
````

````text
One head's query x key matrix at `layer`, cropped to the KV in use.
````

### `delta`

````text
delta(trace_id: str, layer: int, pass_index: int=0) -> dict[str, Any]
````

````text
What block `layer` wrote into the residual stream, per token.
````

### `experts`

````text
experts(trace_id: str, pass_index: int=0) -> dict[str, Any]
````

````text
Mixture-of-experts routing (`moe: False` on a dense model).
````

### `focus`

````text
focus(*, trace_id: str='', layer: int | None=None, position: int | None=None, token_id: int | None=None, stage: str | None=None, step: int | None=None) -> dict[str, Any]
````

````text
Point the user's panes at part of the model (the model-locus bus).
`stage` (`attention`, `ffn`, …) and `step` (a record index) move the
stepper's cursor, which the model explorer follows.
````

### `grid`

````text
grid(trace_id: str, *, lens: str='identity', k: int=5, layers: list[int] | None=None, positions: list[int] | None=None, pass_index: int=0) -> dict[str, Any]
````

````text
The layer x position readout. Carries `verified` — check it before
believing the cells.
````

### `lenses`

````text
lenses(trace_id: str) -> list[dict[str, Any]]
````

````text
Which lenses apply to a trace's model; `identity` is always present.
````

### `locus`

````text
locus() -> dict[str, Any]
````

````text
The model locus most recently set from this side.
````

### `program`

````text
program(trace_id: str, pass_index: int=0) -> list[dict[str, Any]]
````

````text
The records of one pass in execution order — what the stepper steps.
````

### `tensor`

````text
tensor(trace_id: str, name: str, pass_index: int=0) -> Any
````

````text
One record as a numpy array in `[ne3, ne2, ne1, ne0]` order, strides
honoured (`l_out-12`, `kq_soft_max-3`, …).
````

### `traces`

````text
traces(limit: int=20, model_sha: str='', derived_from: str='') -> list[dict[str, Any]]
````

````text
Stored traces, newest first (the `llamacpp_traces` catalog in app.db).
````

### `track`

````text
track(trace_id: str, token_id: int, *, lens: str='identity', pass_index: int=0) -> dict[str, Any]
````

````text
One vocabulary token's rank and logit at every cell — the token pin.
````

### `vocab`

````text
vocab(trace_id: str, query: str, limit: int=20) -> list[dict[str, Any]]
````

````text
Search the traced model's own vocabulary — what a swap must name.
````

## `dash.panes`

### `available`

````text
available() -> Any
````

````text
Every view that can be opened, with id, title, role (document/widget/tool),
default dock (tools), and the region views it hosts.
````

### `close`

````text
close(instance_id: str) -> Any
````

````text
Close an open pane instance by its instance ID (from `open_list()`).
````

### `focus`

````text
focus(instance_id: str) -> Any
````

````text
Bring a pane forward (tab / dock slot / floating card).
````

### `open`

````text
open(view_id: str) -> Any
````

````text
Open a view by its view ID (from `available()`), routed by its role.
````

### `open_list`

````text
open_list() -> Any
````

````text
Pane instances currently open in the active workspace (view id, instanceId,
role, location: center area / dock / floating).
````

## `dash.settings`

### `all`

````text
all() -> dict[str, Any]
````

````text
Every persisted override, as a flat key→value dict.
````

### `get`

````text
get(key: str, default: Any=None) -> Any
````

````text
The value for `key` (the persisted override, else `default`).
````

### `set`

````text
set(key: str, value: Any) -> dict[str, Any]
````

````text
Persist an override for `key`.
````

## `dash.workspaces`

### `create`

````text
create(name: str) -> Any
````

````text
Create a new named workspace and switch to it.
````

### `list`

````text
list() -> Any
````

````text
The workspace tabs and which one is active.
````

### `switch`

````text
switch(workspace_id: str) -> Any
````

````text
Switch to a workspace tab by id (from `list()`).
````
