"""Training subprocess runners (import instances from their submodules:
`runners.script_runner.script_runner`, `runners.manim_runner.manim_runner` —
no package-level re-export, so the submodules stay reachable for tests)."""
